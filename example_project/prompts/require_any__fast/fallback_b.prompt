{{ config(temperature=0.2, output_schema="fallback_result") }}

Try vendor B for emergency restock of {{ product_id }}.

SELECT
    vendor,       -- str
    available,    -- bool
    price_per_unit -- float
FROM attempt
